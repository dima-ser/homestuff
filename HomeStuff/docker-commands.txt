# Run from the solution directory
# If this command gets stuck on "transferring context", restart Docker Desktop (and optionally Visual Studio or entire machine) and try again
docker build --tag dimaser/homestuff -f HomeStuff/Dockerfile .

new command for Linux

docker run --rm -p 5000:80 \
  -v "$PWD/HomeStuff/db":/app/db \
  -e TZ=America/Los_Angeles \
  --workdir=/app \
  dimaser/homestuff:latest

  to push

  docker push dimaser/homestuff:latest